export interface IDistanceRange {
    max: number,
    min: number,
}

export const childDistanceRange : IDistanceRange = {
    max: 0.25,
    min: 0.2,
}

export const parentDistanceRange : IDistanceRange = {
    max: 0.3,
    min: 0.25,
}
